import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.solarengineer.www',
  appName: 'mobile',
  webDir: 'dist/mobile',
  bundledWebRuntime: false
};

export default config;
