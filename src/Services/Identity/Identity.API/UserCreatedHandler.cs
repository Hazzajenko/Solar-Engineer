namespace Identity.API;

public static class UserCreatedHandler
{
    /*public static UserCreated Handle(AppUserEvent eEvent)
    {
        return new UserCreated(eEvent.Id, eEvent.User.Id, ServiceName.Projects);
    }*/
}